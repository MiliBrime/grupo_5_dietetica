<!DOCTYPE html>
<html lang="en">

<%- include ("./partials/head.ejs" , { cssSheet: "login-register", cssSheet1:"", icons: "https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css"} ) %>

<body>
    <a href="/"><img class= "img-lider" src="/img/tvs.png" alt="logo"></a>
    <div class="container">
        <div class="box">
            <div class="back-to-home">
                <a href="/"><i class="fa-solid fa-x" aria-hidden="true"></i></a>
            </div>
            <div class="box-login-register" id="register">
                <div class="top-header">
                    <h3>¡Registrate ahora!</h3>
                    <small>Es un gusto que hagas parte de nuestra familia.</small>
                </div>
                <div class="buttons-icons">
                    <button type="button" class="buttonfb">
                        <h4>Registrate con Facebook</h4>
                    </button>
                    <button type="button" class="buttonfb">
                        <h4>Registrate con Google</h4>
                    </button>
                    <button type="button" class="buttonfb">
                        <h4>Registrate con Apple</h4>
                    </button>
                </div>
                <hr>
                <form class="input-group" method="POST" action="/users/register" enctype="multipart/form-data">  
                    <div class="input-field">
                        <input type="text" class="input-box"
                        id="first_name" name="first_name"  placeholder="Nombre" value= "<%= locals.old ? old.first_name : "" %>">
                        <% if (locals.mensajesDeError && mensajesDeError.first_name) { %> 
                            <%= mensajesDeError.first_name.msg%> <% } %>
                        <div class="error-message" id="error-first_name"></div>
                    </div>
                    <div class="input-field">
                        <input type="text" class="input-box" id="last_name" name="last_name" placeholder="Apellido" value= "<%= locals.old ? old.last_name : "" %>">
                        <% if (locals.mensajesDeError && mensajesDeError.last_name) { %> 
                            <%= mensajesDeError.last_name.msg%> <% } %>
                        <div class="error-message" id="error-last_name"></div>
                    </div>
                    <div class="input-field">
                        <input type="text" class="input-box" id="email" name="email" placeholder="Correo electrónico" value= "<%= locals.old ? old.email : "" %>">
                        <% if (locals.mensajesDeError && mensajesDeError.email) { %> 
                            <%= mensajesDeError.email.msg%> <% } %>
                        <% if (locals.errorMessage) { %>
                            <p class="error-message"><%= locals.errorMessage %></p>
                              <% } %>
                        <div class="error-message" id="error-email"></div>

                    </div>
                    <div class="input-field">
                        <input type="text" class="input-box" id="phone" name="phone" placeholder="Teléfono" value= "<%= locals.old ? old.phone : "" %>">
                        <% if (locals.mensajesDeError && mensajesDeError.phone) { %> 
                            <%= mensajesDeError.phone.msg%> <% } %>
                        <div class="error-message" id="error-phone"></div>
                    </div>
                    <div class="input-field">
                        <input type="password" class="input-box" id="password" name="password" placeholder="Contraseña">
                        <% if (locals.mensajesDeError && mensajesDeError.password) { %> 
                            <%= mensajesDeError.password.msg%> <% } %>
                        <div class="error-message" id="error-password"></div>
                        <div class="eye-area">
                            <div class="eye-box" onclick="myRegPassword()">
                                <i class="fa-regular fa-eye" id="eye-2"></i>
                                <i class="fa-regular fa-eye-slash" id="eye-slash-2"></i>
                            </div>
                        </div>
                    </div>
                    <div class="image">
                        <p class="textImage">Imagen de perfil</p>
                        <input type="file" name= "photo" class="image" id="photo" >
                        <% if (locals.mensajesDeError && mensajesDeError.photo) { %>
                            <%= mensajesDeError.photo.msg %> 
                        <% } %>
                        <div class="error-message" id="error-photo"></div>
                    </div>

                    <div class="input-field">
                        <input type="submit" class="input-submit" value="Registrate" >
                    </div>
                </form>
            </div>
            <div class="switch-register">
                <a href="/users/login" class="login active" onclick="login()">Iniciar Sesión</a>
                <a href="/users/register" class="register" onclick="register()">Registrarse</a>
                <div class="btn-active" id="btn"></div>
            </div>
        </div>
    </div>
   <!--  <script>

        // View Password codes

        function myLogPassword() {

            var a = document.getElementById("logPassword");
            var b = document.getElementById("eye");
            var c = document.getElementById("eye-slash");

            if (a.type === "password") {
                a.type = "text";
                b.style.opacity = "0";
                c.style.opacity = "1";
            } else {
                a.type = "password";
                b.style.opacity = "1";
                c.style.opacity = "0";
            }
        }

        function myRegPassword() {

            var d = document.getElementById("regPassword");
            var b = document.getElementById("eye-2");
            var c = document.getElementById("eye-slash-2");

            if (d.type === "password") {
                d.type = "text";
                b.style.opacity = "0";
                c.style.opacity = "1";
            } else {
                d.type = "password";
                b.style.opacity = "1";
                c.style.opacity = "0";
            }
        }
    </script> -->
        <script src="/js/eyePassword.js"></script>
    </div>
    <script src="/js/registerValidation.js" type="text/javascript"></script>
</body>

</html>