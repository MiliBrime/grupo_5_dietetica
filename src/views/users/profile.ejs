<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ("./partials/head.ejs" , { cssSheet: "profile",cssSheet1:"headerFooter", icons: "https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"} ) %>
</head>

<header>
    <%- include("./partials/header.ejs")%>
</header>

<body>
        <div class="box-profile">
            <div class="contenido input-profile">
                <h2>¡Hola <%=user.first_name%>! </h2>
            </div>
            
            <div class="misDatos">
                <h3>Mis datos:</h3>
            </div>
            
            <form class="formProfile" method="post" enctype="multipart/form-data" action="/users/profile">
                <div class="photo-profile">
                    <img src="/img/users/<%=user.image%>" alt="Foto de perfil">
                </div>
                    <div class="input-file">
                        <label class= "label" for="input-photo">Selecciona una imagen</label>
                        <input class="input-photo" type="file" name="image"
                            accept="image/jpeg, image/png, image,jpg">
                    </div>
                <div class="contenido input-profile">
                    <input type="email" name="email" placeholder="Correo Electrónico"
                    value="<%=user.email %>"
                        class="input input-alt" />
                </div>
                <div class="contenido input-profile">
                    <div class="form-control">
                        <input type="text" name="phone" placeholder="Teléfono celular" value="<%=user.phone %>"
                            class="input input-alt" />
                    </div>
                </div>
                <div class="contenido input-profile">
                    <div class="form-control">
                        <input type="text" name="address" placeholder="Dirección" class="input input-alt" />
                    </div>
                </div>
                <div class="contenido button-save">
                    <button class="btn">
                        Guardar
                    </button>
                </div>
        </form>
        </div>

</body>
<footer>
    <%- include('partials/footer.ejs') %>
</footer>
</html>

<script src="perfilUser.js"></script>